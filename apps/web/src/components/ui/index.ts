/**
 * Local RSC-compatible UI components for the landing page.
 * These avoid importing from @chaosfix/ui which bundles client-side hooks.
 */
export { Button, type ButtonProps } from "./Button.ui";
export { Logo, type LogoProps } from "./Logo.ui";
